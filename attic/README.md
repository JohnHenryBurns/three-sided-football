# The attic

Frozen pages, kept for reference and maintained by nobody. Both predate the engine
extraction and carry their own full copies of it, which is why they are in here: they
polluted every grep of the living code. Their relative `engine.js` script tags no
longer resolve from this directory, and that is fine — a museum piece does not need
to start.
